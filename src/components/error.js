import styled from "styled-components";

import colors from "../utils/colors";

export default styled.span`
    display: block;
    color: ${colors.red};
    text-align: center;
    margin-top: 20px;
`;
